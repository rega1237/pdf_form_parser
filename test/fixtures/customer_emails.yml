# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# We will create emails manually in tests when needed,
# or we can attach these to customer 'two' so customer 'one' is empty.
two:
  address: "test2@example.com"
  primary: true
  customer: two
